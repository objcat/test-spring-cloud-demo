server:
  port: 8082
spring:
  application:
    name: service-objcat-a
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8081/eureka
  #是否需要将自己注册到注册中心
  register-with-eureka: true
  #是否需要搜索服务信息
  fetch-registry: true

